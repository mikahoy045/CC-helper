---
description: Preview the cc-usage status line with sample data across several scenarios
allowed-tools: Bash(node:*)
---

Render the cc-usage status line with sample data so the user can see how it looks:

!`node "${CLAUDE_PLUGIN_ROOT}/scripts/preview.mjs"`

Show the output to the user.
